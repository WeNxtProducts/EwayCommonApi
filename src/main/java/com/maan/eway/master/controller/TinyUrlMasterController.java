package com.maan.eway.master.controller;
/*
*  Copyright (c) 2019. All right reserved
* Created on 2022-08-24 ( Date ISO 2022-08-24 - Time 12:58:26 )
* Generated by Telosys Tools Generator ( version 3.3.0 )
*/

import java.util.Collections;
import java.util.List;
import org.springframework.security.access.prepost.PreAuthorize;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import com.maan.eway.common.req.CommonErrorModuleReq;
import com.maan.eway.common.res.CommonRes;
import com.maan.eway.common.res.DropdownCommonRes;
import com.maan.eway.common.service.impl.FetchErrorDescServiceImpl;
import com.maan.eway.error.Error;
import com.maan.eway.master.req.TinyUrlChangeStatusReq;
import com.maan.eway.master.req.TinyUrlDropdownReq;
import com.maan.eway.master.req.TinyUrlMasterGetReq;
import com.maan.eway.master.req.TinyUrlMasterGetallReq;
import com.maan.eway.master.req.TinyUrlMasterSaveReq;
import com.maan.eway.master.res.TinyUrlMasterRes;
import com.maan.eway.master.service.TinyUrlMasterService;
import com.maan.eway.res.DropDownRes;
import com.maan.eway.res.SuccessRes;
import com.maan.eway.service.PrintReqService;

import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;

/**
 * <h2>CurrencyMasterController</h2>
 */
@RestController
@Api(tags = "MASTER : Tiny URL Master ", description = "API's")
@RequestMapping("/master")
public class TinyUrlMasterController {

	@Autowired
	private TinyUrlMasterService service;

	@Autowired
	private PrintReqService reqPrinter;
	
	@Autowired
	private FetchErrorDescServiceImpl errorDescService ;

	// save
	@PreAuthorize("hasAnyRole('ROLE_ADMIN','ROLE_APPROVER','ROLE_USER')")
	@PostMapping("/inserttinyurl")
	@ApiOperation(value = "This method is Save Tiny Master")
	public ResponseEntity<CommonRes> insertTinyUrl(@RequestBody TinyUrlMasterSaveReq req) {

		reqPrinter.reqPrint(req);
		CommonRes data = new CommonRes();
		List<String> validationCodes = service.validateTinyUrl(req);
		List<Error> validation = null;
		if(validationCodes!=null && validationCodes.size() > 0 ) {
			CommonErrorModuleReq comErrDescReq = new CommonErrorModuleReq();
			comErrDescReq.setBranchCode(req.getBranchCode());
			comErrDescReq.setInsuranceId(req.getCompanyId());
			comErrDescReq.setProductId("99999");
			comErrDescReq.setModuleId("31");
			comErrDescReq.setModuleName("MASTERS");
			
			validation = errorDescService.getErrorDesc(validationCodes ,comErrDescReq);
		}

	
		// validation
		if (validation != null && validation.size() != 0) {
			data.setCommonResponse(null);
			data.setIsError(true);
			data.setErrorMessage(validation);
			data.setMessage("Failed");
			return new ResponseEntity<CommonRes>(data, HttpStatus.OK);

		} else {

			// Get All
			SuccessRes res = service.insertTinyUrl(req);
			data.setCommonResponse(res);
			data.setIsError(false);
			data.setErrorMessage(Collections.emptyList());
			data.setMessage("Success");

			if (res != null) {
				return new ResponseEntity<CommonRes>(data, HttpStatus.CREATED);
			} else {
				return new ResponseEntity<>(null, HttpStatus.BAD_REQUEST);
			}
		}

	}

	// Get Tiny URL Master
	@PreAuthorize("hasAnyRole('ROLE_ADMIN','ROLE_APPROVER','ROLE_USER')")
	@PostMapping("/gettinyurl")
	@ApiOperation("This method is get Tiny Url")
	public ResponseEntity<CommonRes> getTinyUrl(@RequestBody TinyUrlMasterGetReq req) {
		CommonRes data = new CommonRes();
		reqPrinter.reqPrint(req);

		TinyUrlMasterRes res = service.getTinyUrl(req);
		data.setCommonResponse(res);
		data.setErrorMessage(Collections.emptyList());
		data.setIsError(false);
		data.setMessage("Success");

		if (res != null) {
			return new ResponseEntity<CommonRes>(data, HttpStatus.CREATED);
		} else {
			return new ResponseEntity<>(null, HttpStatus.BAD_REQUEST);
		}
	}

	// Get All Tiny URL Master
	@PreAuthorize("hasAnyRole('ROLE_ADMIN','ROLE_APPROVER','ROLE_USER')")
	@PostMapping("/getalltinyurl")
	@ApiOperation("This method is getall Tiny Url")
	public ResponseEntity<CommonRes> getallTinyUrl(@RequestBody TinyUrlMasterGetallReq req) {
		CommonRes data = new CommonRes();
		reqPrinter.reqPrint(req);

		List<TinyUrlMasterRes> res = service.getallTinyUrl(req);
		data.setCommonResponse(res);
		data.setErrorMessage(Collections.emptyList());
		data.setIsError(false);
		data.setMessage("Success");

		if (res != null) {
			return new ResponseEntity<CommonRes>(data, HttpStatus.CREATED);
		} else {
			return new ResponseEntity<>(null, HttpStatus.BAD_REQUEST);
		}
	}

	// Get Active Tiny URL Master
	@PreAuthorize("hasAnyRole('ROLE_ADMIN','ROLE_APPROVER','ROLE_USER')")
	@PostMapping("/getactivetinyurl")
	@ApiOperation("This method is get active Tiny Url")
	public ResponseEntity<CommonRes> getactiveTinyUrl(@RequestBody TinyUrlMasterGetallReq req) {
		CommonRes data = new CommonRes();
		reqPrinter.reqPrint(req);

		List<TinyUrlMasterRes> res = service.getactiveTinyUrl(req);
		data.setCommonResponse(res);
		data.setErrorMessage(Collections.emptyList());
		data.setIsError(false);
		data.setMessage("Success");

		if (res != null) {
			return new ResponseEntity<CommonRes>(data, HttpStatus.CREATED);
		} else {
			return new ResponseEntity<>(null, HttpStatus.BAD_REQUEST);
		}
	}

	
	// Change Status Tiny URL Master
	@PreAuthorize("hasAnyRole('ROLE_ADMIN','ROLE_APPROVER','ROLE_USER')")
	@PostMapping("/changestatustinyurl")
	@ApiOperation("This method is Change Status Tiny Url")
	public ResponseEntity<CommonRes> changestatusTinyUrl(@RequestBody TinyUrlChangeStatusReq req) {
		CommonRes data = new CommonRes();
		reqPrinter.reqPrint(req);

		SuccessRes res = service.changestatusTinyUrl(req);
		data.setCommonResponse(res);
		data.setErrorMessage(Collections.emptyList());
		data.setIsError(false);
		data.setMessage("Success");

		if (res != null) {
			return new ResponseEntity<CommonRes>(data, HttpStatus.CREATED);
		} else {
			return new ResponseEntity<>(null, HttpStatus.BAD_REQUEST);
		}
	}

	
	
	// Tiny Url Master Drop Down Type
	@PreAuthorize("hasAnyRole('ROLE_APPROVER','ROLE_USER','ROLE_ADMIN')")
	@PostMapping(value="/dropdown/tinyurl",produces = "application/json")
	@ApiOperation(value = "This method is get Tiny URL Drop Down")

	public ResponseEntity<DropdownCommonRes> getTinyUrlDropdown(@RequestBody TinyUrlDropdownReq req) {

		DropdownCommonRes data = new DropdownCommonRes();

		// Save
		List<DropDownRes> res = service.getTinyUrlDropdown(req);
		data.setCommonResponse(res);
		data.setIsError(false);
		data.setErrorMessage(Collections.emptyList());
		data.setMessage("Success");

		if (res != null) {
			return new ResponseEntity<DropdownCommonRes>(data, HttpStatus.CREATED);
		} else {
			return new ResponseEntity<>(null, HttpStatus.BAD_REQUEST);
		}

	}	

	
}
