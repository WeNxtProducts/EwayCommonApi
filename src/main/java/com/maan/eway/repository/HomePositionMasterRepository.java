/*
 * Java domain class for entity "HomePositionMaster" 
 * Created on 2022-09-06 ( Date ISO 2022-09-06 - Time 11:31:27 )
 * Generated by Telosys Tools Generator ( version 3.3.0 )
 */

/*
* Created on 2022-09-06 ( 11:31:27 )
* Generated by Telosys ( http://www.telosys.org/ ) version 3.3.0
*/

package com.maan.eway.repository;

import java.math.BigDecimal;

import java.util.List;

import jakarta.transaction.Transactional;

import com.maan.eway.bean.HomePositionMaster;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.JpaSpecificationExecutor;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;

import com.maan.eway.bean.HomePositionMasterId;


/**
 * <h2>HomePositionMasterRepository</h2>
 *
 * createdAt : 2022-09-06 - Time 11:31:27
 * <p>
 * Description: "HomePositionMaster" Repository
 */

@Transactional
public interface HomePositionMasterRepository
		extends JpaRepository<HomePositionMaster, HomePositionMasterId>, JpaSpecificationExecutor<HomePositionMaster> {

	HomePositionMaster findByQuoteNo(String quoteNo);

	Long countByQuoteNo(String quoteNo);

	@Transactional
	void deleteByQuoteNo(String quoteNo);

	Long countByRequestReferenceNo(String requestReferenceNo);

	List<HomePositionMaster> findByRequestReferenceNo(String requestReferenceNo);

	HomePositionMaster findByPolicyNoAndStatusAndCompanyIdAndProductId(String policyNo, String status, String companyId,
			Integer productId);

	List<HomePositionMaster> findByQuoteNoAndProductId(String quoteNo, Integer productId);

	List<HomePositionMaster> findByRequestReferenceNoAndProductId(String requestReferenceNo, Integer productId);

	HomePositionMaster findByPolicyNo(String policyNo);

	HomePositionMaster findByPolicyNoAndStatus(String policyNo, String string);

	Long countByQuoteNoNotAndEndtCountAndOriginalPolicyNo(String quoteNo, Integer bigDecimal, String originalPolicyNo);

	@Transactional
	void deleteByQuoteNoNotAndEndtCountAndOriginalPolicyNo(String quoteNo, Integer bigDecimal, String originalPolicyNo);

	HomePositionMaster findTop1ByPolicyNo(String policyNo);

	List<HomePositionMaster> findAllByOrderByEntryDateDesc();

	List<HomePositionMaster> findAllByQuoteNoIn(List<String> quoteNo);

	@Query(value = """
			  SELECT
			  policy_no
			 ,entry_date
			 ,no_of_vehicles
			 ,quote_no
			 ,customer_id
			 ,REQUEST_REFERENCE_NO
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Policy_Detail
			    WHERE DATE(request_time) BETWEEN STR_TO_DATE(:effectiveStartDate, '%d/%m/%Y') AND STR_TO_DATE(:effectiveEndDate, '%d/%m/%Y'))
			  Yi_Policy_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Section_Detail
			    WHERE DATE(request_time) BETWEEN STR_TO_DATE(:effectiveStartDate, '%d/%m/%Y') AND STR_TO_DATE(:effectiveEndDate, '%d/%m/%Y'))
			  Yi_Section_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM PGIT_POL_RISK_ADDL_INFO_01
			    WHERE DATE(request_time) BETWEEN STR_TO_DATE(:effectiveStartDate, '%d/%m/%Y') AND STR_TO_DATE(:effectiveEndDate, '%d/%m/%Y'))
			  PGIT_POL_RISK_ADDL_INFO_01
			 ,(SELECT
			      COUNT(*)
			    FROM Mot_Driver_Detail
			    WHERE DATE(request_time) BETWEEN STR_TO_DATE(:effectiveStartDate, '%d/%m/%Y') AND STR_TO_DATE(:effectiveEndDate, '%d/%m/%Y'))
			  Mot_Driver_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Cover_Detail
			    WHERE DATE(request_time) BETWEEN STR_TO_DATE(:effectiveStartDate, '%d/%m/%Y') AND STR_TO_DATE(:effectiveEndDate, '%d/%m/%Y'))
			  Yi_Cover_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM Mot_Comm_Discount_Detail
			    WHERE DATE(request_time) BETWEEN STR_TO_DATE(:effectiveStartDate, '%d/%m/%Y') AND STR_TO_DATE(:effectiveEndDate, '%d/%m/%Y'))
			  Mot_Comm_Discount_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Vat_Detail
			    WHERE DATE(request_time) BETWEEN STR_TO_DATE(:effectiveStartDate, '%d/%m/%Y') AND STR_TO_DATE(:effectiveEndDate, '%d/%m/%Y'))
			  Yi_Vat_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Charge_Detail
			    WHERE DATE(request_time) BETWEEN STR_TO_DATE(:effectiveStartDate, '%d/%m/%Y') AND STR_TO_DATE(:effectiveEndDate, '%d/%m/%Y'))
			  Yi_Charge_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Prem_Cal
			    WHERE DATE(request_time) BETWEEN STR_TO_DATE(:effectiveStartDate, '%d/%m/%Y') AND STR_TO_DATE(:effectiveEndDate, '%d/%m/%Y'))
			  Yi_Prem_Cal
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Policy_Approval
			    WHERE DATE(request_time) BETWEEN STR_TO_DATE(:effectiveStartDate, '%d/%m/%Y') AND STR_TO_DATE(:effectiveEndDate, '%d/%m/%Y'))
			  Yi_Policy_Approval
			 ,(SELECT
			      COUNT(*)
			    FROM Credit_Limit_Detail
			    WHERE DATE(request_time) BETWEEN STR_TO_DATE(:effectiveStartDate, '%d/%m/%Y') AND STR_TO_DATE(:effectiveEndDate, '%d/%m/%Y'))
			  Credit_Limit_Detail
			FROM home_position_master
			WHERE STATUS = 'P'
			AND DATE(entry_date) BETWEEN STR_TO_DATE(:effectiveStartDate, '%d/%m/%Y')
			AND STR_TO_DATE(:effectiveEndDate, '%d/%m/%Y')
							    """, nativeQuery = true)

	public List<Object[]> checkPolicyDetailsByEffectiveDate(@Param("effectiveStartDate") String effectiveStartDate, 
			                                  @Param("effectiveEndDate") String effectiveEndDate);

	@Query(value = """
			  SELECT
			  policy_no
			 ,entry_date
			 ,no_of_vehicles
			 ,quote_no
			 ,customer_id
			 ,REQUEST_REFERENCE_NO
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Policy_Detail
			    WHERE quotation_policy_no = :policyNo)
			  Yi_Policy_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Section_Detail
			    WHERE quotation_policy_no = :policyNo)
			  Yi_Section_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM PGIT_POL_RISK_ADDL_INFO_01
			    WHERE quotation_policy_no = :policyNo)
			  PGIT_POL_RISK_ADDL_INFO_01
			 ,(SELECT
			      COUNT(*)
			    FROM Mot_Driver_Detail
			    WHERE quotation_policy_no = :policyNo)
			  Mot_Driver_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Cover_Detail
			    WHERE quotation_policy_no = :policyNo)
			  Yi_Cover_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM Mot_Comm_Discount_Detail
			    WHERE quotation_policy_no = :policyNo)
			  Mot_Comm_Discount_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Vat_Detail
			    WHERE quotation_policy_no = :policyNo)
			  Yi_Vat_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Charge_Detail
			    WHERE quotation_policy_no = :policyNo)
			  Yi_Charge_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Prem_Cal
			    WHERE quotation_policy_no = :policyNo)
			  Yi_Prem_Cal
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Policy_Approval
			    WHERE quotation_policy_no = :policyNo)
			  Yi_Policy_Approval
			 ,(SELECT
			      COUNT(*)
			    FROM Credit_Limit_Detail
			    WHERE customer_code = :customerId)
			  Credit_Limit_Detail
			FROM home_position_master
			WHERE STATUS = 'P'
			""", nativeQuery = true)
	public List<Object[]> checkPolicyDetailsByPolicyNo(@Param("policyNo") String policyNo,
			@Param("customerId") String customerId);

	@Query(value = """
			SELECT
			  policy_no
			 ,entry_date
			 ,no_of_vehicles
			 ,quote_no
			 ,customer_id
			 ,REQUEST_REFERENCE_NO
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Policy_Detail
			    WHERE requestreferenceno = :requestReferenceNo)
			  Yi_Policy_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Section_Detail
			    WHERE requestreferenceno = :requestReferenceNo)
			  Yi_Section_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM PGIT_POL_RISK_ADDL_INFO_01
			    WHERE request_reference_no = :requestReferenceNo)
			  PGIT_POL_RISK_ADDL_INFO_01
			 ,(SELECT
			      COUNT(*)
			    FROM Mot_Driver_Detail
			    WHERE requestreferenceno = :requestReferenceNo)
			  Mot_Driver_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Cover_Detail
			    WHERE requestreferenceno = :requestReferenceNo)
			  Yi_Cover_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM Mot_Comm_Discount_Detail
			    WHERE requestreferenceno = :requestReferenceNo)
			  Mot_Comm_Discount_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Vat_Detail
			    WHERE requestreferenceno = :requestReferenceNo)
			  Yi_Vat_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Charge_Detail
			    WHERE requestreferenceno = :requestReferenceNo)
			  Yi_Charge_Detail
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Prem_Cal
			    WHERE requestreferenceno = :requestReferenceNo)
			  Yi_Prem_Cal
			 ,(SELECT
			      COUNT(*)
			    FROM Yi_Policy_Approval
			    WHERE requestreferenceno = :requestReferenceNo)
			  Yi_Policy_Approval
			 ,(SELECT
			      COUNT(*)
			    FROM Credit_Limit_Detail
			    WHERE requestreferenceno = :requestReferenceNo)
			  Credit_Limit_Detail
			FROM home_position_master
			WHERE STATUS = 'P'
			""", nativeQuery = true)
	public List<Object[]> checkPolicyDetailsByPolicyNoRequestReferenceNo(
			@Param("requestReferenceNo") String requestReferenceNo);

	List<HomePositionMaster> findAllByPolicyNoAndStatus(String policyNo, String string);

	List<HomePositionMaster> findAllByPolicyNo(String policyNo);

}
